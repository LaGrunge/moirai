// Dashboard Configuration
// Generated from environment variables using: envsubst < config.js.template > config.js

window.DASHBOARD_CONFIG = {
    servers: [
        { name: "${CI_SERVER_NAME}", url: "${CI_SERVER_URL}", token: "${CI_SERVER_TOKEN}", type: "${CI_SERVER_TYPE:-woodpecker}" }
    ],
    defaultTheme: "${DASHBOARD_THEME:-light}",
    autoSelectServer: true,
    autoSelectRepo: true
};
